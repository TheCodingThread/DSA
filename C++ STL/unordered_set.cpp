#include<bits/stdc++.h>
using namespace std;

int main(){
    unordered_set<int> us; //initialisation
    // Unordered Set is similar to set but the elements are not stored in a sorted manner, that is, the elemets are stored in a random manner

    // All the operations are same as set but the time complexity of unordered set is O(1) for all operations. Also the lower_bound and upper_bound functions are not available in this set
    return 0;
}