#include<bits/stdc++.h>
using namespace std;

int main(){
    unordered_map<int, int> m; //initialisation
    //Here keys are unique but in random order(unsorted)
    //unordered_map works in O(1) time complexity

    return 0;
}